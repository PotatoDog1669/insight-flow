Generate strict JSON only for the `paper_note` stage.
Do not add markdown fences, explanations, or non-JSON text.

This prompt is a placeholder contract for the paper note stage.
It will be expanded in a follow-up task.

Selected paper payload:
$paper_json
