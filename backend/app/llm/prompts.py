"""Prompt 模板管理"""

# AI 初筛 Prompt
FILTER_PROMPT = """你是一个 AI 信息筛选助手。请判断以下内容是否满足这两个条件：
1. 与 AI / 机器学习 / 深度学习相关
2. 有报道价值（非单纯使用教程或广告）

标题: {title}
内容摘要: {content}

回复 "YES" 或 "NO"。"""

# 摘要生成 Prompt
SUMMARY_PROMPT = """请为以下内容生成一句不超过 50 个字的中文摘要：

标题: {title}
内容: {content}

要求：简洁、信息密度高、突出核心价值。只输出摘要文本。"""

# 关键词提取 Prompt
KEYWORD_PROMPT = """请从以下内容中提取 3~5 个关键词。如果以下已有关键词与内容相关，优先使用：

已有关键词库: {existing_keywords}

标题: {title}
内容: {content}

以 JSON 数组格式返回，如：["关键词1", "关键词2"]"""

# 智能打分 Prompt
SCORE_PROMPT = """请评估以下 AI 相关资讯的价值，从 0.0 到 1.0 打分。
评分维度：新颖度（30%）、影响力（40%）、技术深度（30%）。

标题: {title}
内容: {content}

只返回一个数字（保留两位小数），如：0.75"""
