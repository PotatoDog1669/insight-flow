{% if paper_mode | default("digest", true) == "note" -%}
{% include "reports/paper/note/v1.md.j2" %}
{% else -%}
{% set digest_title = title | default("", true) | trim -%}
{% if digest_title -%}
# {{ digest_title }}
{% elif date | default("", true) | trim -%}
# {{ date | trim }} 论文推荐
{% else -%}
# 论文推荐
{% endif -%}

{% if summary | default("", true) | trim -%}
## 本期导读

{{ summary | trim }}

{% endif -%}
## 推荐论文

{% for paper in papers | default([]) -%}
---
### {{ loop.index }}. {{ paper.title | default("未命名论文") }}

- 作者：{{ paper.authors | default("N/A") }}
- 机构：{{ paper.affiliations | default("N/A") }}
{% if paper.figure | default("", true) | trim -%}
- 核心图：![核心图]({{ paper.figure }})
{% endif -%}
- 一句话判断：{{ paper.one_line | default("") }}
- 核心问题：{{ paper.problem | default("") }}
- 核心方法：{{ paper.method | default("") }}
- 关键结果：{{ paper.result | default("") }}
{%- set importance = paper.importance | default("", true) | trim -%}
{%- if importance and importance != "值得重点关注" -%}
- 为什么重要：{{ importance }}
{%- endif -%}
{%- set reading_level = paper.reading_level | default("", true) | trim -%}
{%- if reading_level and reading_level != "必读" -%}
- 阅读建议：{{ reading_level }}
{%- endif -%}
{%- set detail_link = paper.detail_link | default("", true) | trim -%}
{%- if detail_link -%}
- 详细笔记：{{ detail_link }}
{%- endif -%}

{% endfor -%}
{% endif -%}
